[tool.poetry]
name = "arc2control"
version = "0.1.0"
description = "ArC2 Control Panel"
authors = ["Spyros Stathopoulos <devel@arc-instruments.co.uk>"]
repository = "https://github.com/arc-instruments/arc2control"
exclude = ["venv-pyarc2-update.py"]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.18"
PyQt6 = "^6.1"
pyqtgraph = "^0.12.3"
toml = "^0.10"
h5py = "^3.2"

[tool.poetry.extras]
pyarc2 = ["pyarc2"]

[tool.poetry.dev-dependencies]
build = "^0.7.0"

[build-system]
requires = ["setuptools>=40.8.0", "wheel", "PyQt6"]
build-backend = "setuptools.build_meta"

